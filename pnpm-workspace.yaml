allowBuilds:
  sharp: false
  unrs-resolver: false
overrides:
  # Patch the qs version pulled through shadcn's MCP/Express toolchain.
  qs: 6.16.0
  # GHSA js-yaml CPU-exhaustion advisory; transitive via openapi-typescript,
  # commitlint and shadcn, so there is no direct dependency to bump.
  js-yaml: 4.3.2
