import { FeaturePlaceholder } from "@/components/feedback/feature-placeholder";

export default function JobsPage() {
  return <FeaturePlaceholder eyebrow="Authenticated area" title="Jobs" description="Job discovery, offers, and work state transitions will be added here when the backend endpoints are available." />;
}
