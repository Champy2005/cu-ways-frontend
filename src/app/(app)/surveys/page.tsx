import { FeaturePlaceholder } from "@/components/feedback/feature-placeholder";

export default function SurveysPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Authenticated area"
      title="Surveys"
      description="Survey discovery and response workflows will be added here. The route and authenticated layout are ready for the feature module."
    />
  );
}
