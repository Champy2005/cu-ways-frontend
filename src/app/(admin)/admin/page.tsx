import { FeaturePlaceholder } from "@/components/feedback/feature-placeholder";

export default function AdminPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Administrator area"
      title="Admin workspace"
      description="Manage platform users and future operational workflows from this protected area."
    />
  );
}
